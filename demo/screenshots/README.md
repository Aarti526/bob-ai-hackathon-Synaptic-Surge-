# Screenshots

Add at least 3 screenshots of the running application, named sequentially, e.g.:

```
01-overview-dashboard.png
02-incident-table.png
03-bluf-detail.png
```

Run `streamlit run app/main.py` from `src/` (see `docs/setup-guide.md`) and capture the
Overview page, the incident table, and an expanded incident BLUF.
